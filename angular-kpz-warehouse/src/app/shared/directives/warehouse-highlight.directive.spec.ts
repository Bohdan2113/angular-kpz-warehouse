import { WarehouseHighlightDirective } from './warehouse-highlight.directive';

describe('WarehouseHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new WarehouseHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
